---
description: Verify the current BookNote code without adding features
agent: build
---

Read `AGENTS.md` and inspect available project scripts/configuration.

Verify the current implementation. Run the relevant commands that exist, such as typecheck, build, lint, tests, and/or Tauri checks where appropriate and available.

If verification fails because of code/config currently in the repository, diagnose the actual cause and make only the smallest fixes needed to restore the existing intended behavior.

Do not add new product features and do not advance roadmap phases.

Report:

- Commands run.
- Pass/fail outcome.
- Fixes made, if any.
- Remaining warnings or environment-specific blockers.
